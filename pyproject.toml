[project]
name = "data-loader"
version = "0.2.3"
requires-python = ">=3.14"
dependencies = [
    "adbc-driver-postgresql>=1.11.0",
    "apache-airflow[graphviz]>=3.2.2",
    "asyncpg>=0.31.0",
    "connectorx>=0.4.5",
    "dlt[duckdb,postgres,sql-database]>=1.27.0",
    "fsspec>=2026.4.0",
    "google-genai>=2.4.0",
    "lyricsgenius>=3.12.1",
    "numpy>=2.4.6",
    "polars>=1.40.1",
    "pyarrow>=24.0.0",
    "rapidfuzz>=3.14.5",
    "scipy>=1.17.1",
    "tqdm>=4.67.3",
]

[dependency-groups]
dev = [
    "marimo>=0.23.9",
]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "data_loader"
module-root = ""

[tool.ruff.format]
quote-style = "single"
