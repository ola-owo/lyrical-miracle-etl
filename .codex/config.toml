# hide .env files in writeable workspaces
default_permissions = "workspace"

[permissions.workspace.filesystem]
":project_roots" = { "." = "write", "**/*.env" = "none", "**/secrets.toml" = "none" }
glob_scan_max_depth = 3
